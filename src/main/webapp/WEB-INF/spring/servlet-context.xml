<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--  servlet-context.xml 파일에서는 컨트롤러 관련 설정만 추가  -->
    <!--  이전에 servlet-context.xml 파일에 포함된 설정 클래스를 빈으로 등록하는 작업은 root-context에서 담당  -->
    <!--  즉, 루트 컨테이너와 서블릿 컨테이너의 역할을 명확히 분리하고, 필터에 기반한 스프링 시큐리티 모듈이 그 사이에서 동작하는 구조가 되도록 설정해야 함   -->
    <context:component-scan base-package="com.ssg.meowcoffee.controller"/>

    <mvc:annotation-driven conversion-service="conversionService"/>
    <mvc:resources mapping="/resources/**" location="classpath:/static/"/>

    <bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
        <property name="formatters">
            <set>
                <bean class="com.ssg.meowcoffee.controller.formatter.LocalDateTimeFormatter"/>
                <bean class="com.ssg.meowcoffee.controller.formatter.CheckboxFormatter"/>
                <bean class="com.ssg.meowcoffee.controller.formatter.LocalDateFormatter"/>
            </set>
        </property>
    </bean>
</beans>